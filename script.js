import{initializeApp}from"https://www.gstatic.com/firebasejs/10.12.2/firebase-app.js";import{getDatabase,ref,onValue,set,get}from"https://www.gstatic.com/firebasejs/10.12.2/firebase-database.js";import{firebaseConfig}from"./firebase-config.js";
const app=initializeApp(firebaseConfig),db=getDatabase(app),DEF={batteryWarn:12.2,batteryAlarm:11.8,oilPressureWarn:2,oilPressureAlarm:1.2,oilTempWarn:110,oilTempAlarm:125,cylTempWarn:180,cylTempAlarm:220};let limits=DEF,h={oilTemp:[],cylTemp:[]},active=new Set(),last=0,lastPos=null,first=true,currentLive=null,lastMaxResetAt=0;const HMAX=60,AMAX=30;
let map=L.map("map").setView([48.2,16.3],15);L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png",{maxZoom:19,attribution:"&copy; OpenStreetMap"}).addTo(map);let marker=L.marker([48.2,16.3],{icon:L.icon({iconUrl:"tractor.png",iconSize:[76,76],iconAnchor:[38,38],className:"leaflet-custom-tractor"})}).addTo(map);
setupNotifications();onValue(ref(db,"tracker/settings"),s=>limits={...DEF,...(s.val()||{})});onValue(ref(db,"tracker/maxValues"),s=>renderMax(s.val()||{}));onValue(ref(db,"tracker/alarmHistory"),s=>renderHist(s.val()||[]));onValue(ref(db,"tracker/commands/resetMaxValues"),s=>{let cmd=s.val();if(cmd&&cmd.resetAt&&cmd.resetAt!==lastMaxResetAt){lastMaxResetAt=cmd.resetAt;resetMaxFromCurrentLive(cmd.resetAt);}});
onValue(ref(db,"tracker/live"),s=>{let d=s.val();currentLive=d;if(!d||d.lat===undefined||d.lng===undefined){offline("Keine Daten");return}let lat=Number(d.lat),lng=Number(d.lng);if(isNaN(lat)||isNaN(lng)){offline("GPS ungültig");return}last=Date.now();lastPos={lat,lng};status("Online",1);conn(d);let speed=num(d.speed_kmh),bat=num(d.battery_v),rpm=num(d.rpm),op=num(d.oil_pressure),ot=num(d.oil_temp),ct=num(d.cylinder_temp),hd=num(d.hdop);txt("speed",speed!=null?speed.toFixed(1):"---");txt("sat",d.satellites??"---");txt("lastUpdateSmall",new Date().toLocaleTimeString("de-AT"));txt("battery",bat!=null?bat.toFixed(1):"---");txt("rpm",rpm!=null?Math.round(rpm):"---");txt("oilpressure",op!=null?op.toFixed(1):"---");txt("oiltemp",ot!=null?Math.round(ot):"---");txt("cyltemp",ct!=null?Math.round(ct):"---");gpsq(hd);maxv({speed,rpm,oilTemp:ot,cylTemp:ct,oilPressure:op,battery:bat});let a=[];alarm("battery","batteryIcon",bat,"low",limits.batteryWarn,limits.batteryAlarm,"Batteriespannung","V","battery",a);alarm("oilpressure","oilpressureIcon",op,"low",limits.oilPressureWarn,limits.oilPressureAlarm,"Öldruck","bar","oilPressure",a);alarm("oiltemp","oiltempIcon",ot,"high",limits.oilTempWarn,limits.oilTempAlarm,"Öltemperatur","°C","oilTemp",a);alarm("cyltemp","cyltempIcon",ct,"high",limits.cylTempWarn,limits.cylTempAlarm,"Zylindertemperatur","°C","cylTemp",a);banner(a);hist(a);add("oilTemp",ot);add("cylTemp",ct);chart("oilTempChart",h.oilTemp,"°C");chart("cylTempChart",h.cylTemp,"°C");marker.setLatLng([lat,lng]);first?(map.setView([lat,lng],17),first=false):map.panTo([lat,lng]);let mb=document.getElementById("mapsButton");mb.href=`https://www.google.com/maps?q=${lat},${lng}`;mb.classList.remove("disabled")});
setInterval(()=>{if(!last)offline("Keine Daten");else if((Date.now()-last)/1000>10)offline("Offline");else status("Online",1)},1000);
function txt(i,v){document.getElementById(i).innerText=v}function num(v){if(v===undefined||v===null||v==="")return null;v=Number(v);return isNaN(v)?null:v}function status(t,on){let s=document.getElementById("status"),i=document.getElementById("statusIcon");s.innerText=t;s.className="value "+(on?"online":"offline");i.classList.remove("online","offline");i.classList.add(on?"online":"offline")}function conn(d){let r=num(d.wifi_rssi);txt("wifiRssi",r!=null?Math.round(r):"---");txt("connection",r==null?"Online":r>-60?"Sehr gut":r>-75?"Gut":"Schwach")}function gpsq(hd){let i=document.getElementById("gpsQualityIcon");i.classList.remove("green","yellow","red","purple");if(hd==null){txt("gpsQuality","---");txt("hdop","---");i.classList.add("purple");return}txt("hdop",hd.toFixed(1));txt("gpsQuality",hd<=1.5?"Sehr gut":hd<=3?"Mittel":"Schlecht");i.classList.add(hd<=1.5?"green":hd<=3?"yellow":"red")}function offline(t){status(t,0);["speed","sat","battery","rpm","oilpressure","oiltemp","cyltemp","gpsQuality","hdop"].forEach(i=>txt(i,"---"));["battery","oilpressure","oiltemp","cyltemp"].forEach(clear);banner([]);if(!last)txt("lastUpdateSmall","---");let mb=document.getElementById("mapsButton");if(lastPos){mb.href=`https://www.google.com/maps?q=${lastPos.lat},${lastPos.lng}`;mb.classList.remove("disabled")}else{mb.href="#";mb.classList.add("disabled")}}function alarm(vid,iid,v,dir,w,a,label,unit,key,out){let ve=document.getElementById(vid),ie=document.getElementById(iid),c=ve.closest(".card");clear(vid);if(v==null)return;let al=dir=="low"?v<=a:v>=a,wa=dir=="low"?v<=w&&!al:v>=w&&!al;if(al){c.classList.add("alarm-card");ve.classList.add("alarm-color");ie.classList.add("alarm-color");out.push({key:key+"_alarm",level:"alarm",text:`${label} kritisch: ${fmt(v)} ${unit}`})}else if(wa){c.classList.add("warning-card");ve.classList.add("warn-color");ie.classList.add("warn-color");out.push({key:key+"_warning",level:"warning",text:`${label} Warnung: ${fmt(v)} ${unit}`})}}function clear(id){let ve=document.getElementById(id);if(!ve)return;let c=ve.closest(".card"),ic=c.querySelector(".icon");c.classList.remove("warning-card","alarm-card");ve.classList.remove("warn-color","alarm-color");ic&&ic.classList.remove("warn-color","alarm-color")}function banner(a){let b=document.getElementById("alarmBanner"),t=document.getElementById("alarmText");if(!a.length){b.classList.add("hidden");return}b.classList.remove("hidden","warning");if(!a.some(x=>x.level=="alarm"))b.classList.add("warning");t.innerText=a.map(x=>x.text).join(" · ")}async function resetMaxFromCurrentLive(resetAt){if(!currentLive)return;let n={maxSpeed:num(currentLive.speed_kmh),maxRpm:num(currentLive.rpm),maxOilTemp:num(currentLive.oil_temp),maxCylTemp:num(currentLive.cylinder_temp),minOilPressure:num(currentLive.oil_pressure),minBattery:num(currentLive.battery_v),resetAt:resetAt};await set(ref(db,"tracker/maxValues"),n)}async function maxv(v){let s=await get(ref(db,"tracker/maxValues")),m=s.val()||{},n={...m};if(v.speed!=null)n.maxSpeed=max(n.maxSpeed,v.speed);if(v.rpm!=null)n.maxRpm=max(n.maxRpm,v.rpm);if(v.oilTemp!=null)n.maxOilTemp=max(n.maxOilTemp,v.oilTemp);if(v.cylTemp!=null)n.maxCylTemp=max(n.maxCylTemp,v.cylTemp);if(v.oilPressure!=null)n.minOilPressure=min(n.minOilPressure,v.oilPressure);if(v.battery!=null)n.minBattery=min(n.minBattery,v.battery);if(JSON.stringify(n)!=JSON.stringify(m))set(ref(db,"tracker/maxValues"),n)}async function hist(a){let cur=new Set(a.map(x=>x.key)),neu=a.filter(x=>!active.has(x.key));active=cur;if(!neu.length)return;let s=await get(ref(db,"tracker/alarmHistory")),ar=s.val()||[];neu.forEach(x=>{ar.unshift({time:new Date().toLocaleTimeString("de-AT"),level:x.level,text:x.text});notify("MF35X Alarm",x.text)});set(ref(db,"tracker/alarmHistory"),ar.slice(0,AMAX))}function renderHist(ar){let c=document.getElementById("alarmHistory");if(!ar.length){c.innerHTML='<div class="empty-history">Noch keine Alarme.</div>';return}c.innerHTML=ar.map(e=>`<div class="alarm-entry ${e.level=="warning"?"warning-entry":""}"><div class="alarm-time">${e.time}</div><div class="alarm-message">${e.text}</div></div>`).join("")}function renderMax(m){txt("maxSpeed",m.maxSpeed!=null?Number(m.maxSpeed).toFixed(1):"---");txt("maxRpm",m.maxRpm!=null?Math.round(m.maxRpm):"---");txt("maxOilTemp",m.maxOilTemp!=null?Math.round(m.maxOilTemp):"---");txt("maxCylTemp",m.maxCylTemp!=null?Math.round(m.maxCylTemp):"---");txt("minOilPressure",m.minOilPressure!=null?Number(m.minOilPressure).toFixed(1):"---");txt("minBattery",m.minBattery!=null?Number(m.minBattery).toFixed(1):"---")}function max(c,v){return c==null?v:Math.max(c,v)}function min(c,v){return c==null?v:Math.min(c,v)}function fmt(v){return Number.isInteger(v)?v:v.toFixed(1)}function add(k,v){if(v==null)return;h[k].push(v);if(h[k].length>HMAX)h[k].shift()}function chart(id,val,unit){let ca=document.getElementById(id),x=ca.getContext("2d"),w=ca.width,hh=ca.height;x.clearRect(0,0,w,hh);x.strokeStyle="#444";x.beginPath();x.moveTo(35,10);x.lineTo(35,hh-25);x.lineTo(w-10,hh-25);x.stroke();if(val.length<2){x.fillStyle="#aaa";x.font="14px Arial";x.fillText("Warte auf Daten...",45,80);return}let mn=Math.min(...val),mx=Math.max(...val),r=mx-mn||1;x.strokeStyle="#2e9bff";x.lineWidth=3;x.beginPath();val.forEach((v,i)=>{let xx=35+i/(HMAX-1)*(w-50),yy=10+(mx-v)/r*(hh-40);i?x.lineTo(xx,yy):x.moveTo(xx,yy)});x.stroke()}function setupNotifications(){let b=document.getElementById("requestNotifications"),tog=document.getElementById("notificationToggle"),s=document.getElementById("notifyStatus");tog.checked=localStorage.getItem("mf35xNotificationsEnabled")==="true";up();if(!("Notification"in window)){s.innerText="Nicht unterstützt";b.disabled=true;tog.disabled=true;return}b.onclick=async()=>{let p=await Notification.requestPermission();tog.checked=p==="granted";localStorage.setItem("mf35xNotificationsEnabled",tog.checked?"true":"false");up();if(tog.checked)notify("MF35X Tracker","Benachrichtigungen sind aktiviert.")};tog.onchange=async()=>{if(tog.checked&&Notification.permission!=="granted"){let p=await Notification.requestPermission();if(p!=="granted")tog.checked=false}localStorage.setItem("mf35xNotificationsEnabled",tog.checked?"true":"false");up()};function up(){if(!("Notification"in window)){s.innerText="Nicht unterstützt";return}s.innerText=localStorage.getItem("mf35xNotificationsEnabled")==="true"?"Ein":"Aus";b.style.display=Notification.permission==="granted"?"none":"inline-block"}}function notify(title,body){if(("Notification"in window)&&Notification.permission==="granted"&&localStorage.getItem("mf35xNotificationsEnabled")==="true")new Notification(title,{body,icon:"tractor.png"})}